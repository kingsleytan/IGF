<h1>View All: User Attendances</h1>

<table>
  <tr>
    <th>Username</th>
    <th>Upline</th>
    <th>Location</th>
    <th>Date</th>
  </tr>
  <% @turnouts.each do |t| %>
  <tr>
    <td><%= User.find_by(id: t.user_id).username %></td>
    <td><%= User.find_by(id: t.user_id).upline %></td>
    <td><%= t.location %></td>
    <td><%= t.created_at.strftime("%m/%d/%Y, %a, %I:%M%p") %></td>
    <td>
      <%= link_to "Edit", edit_turnout_path(t), class: "btn btn-success" %>
    </td>
    <td>
      <%= link_to "Delete", turnout_path(t), method: :delete, class: "btn btn-danger", data: { confirm: "Delete?"} %>
    </td>
  </tr>
  <% end %>
</table>
